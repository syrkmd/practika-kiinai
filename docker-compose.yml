services:
  postgres:
    image: postgres:17
    container_name: vorstu-postgres
    restart: unless-stopped

    env_file:
      - .env

    ports:
      - "5432:5432"

    volumes:
      - vorstu_postgres_data:/var/lib/postgresql/data

volumes:
  vorstu_postgres_data: